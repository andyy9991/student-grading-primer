server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /students {
        proxy_pass http://backend:5000;
    }

    location /stats {
        proxy_pass http://backend:5000;
    }
}